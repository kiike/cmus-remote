[main]
# Define the relative location to the web server. For instance,
# if you plan to access the webapp from http://example.com/cmus-remote,
# enter /cmus-remote. If it has to be in the root, just leave it unchanged.
path = /
